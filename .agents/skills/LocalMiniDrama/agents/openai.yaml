interface:
  display_name: "LocalMiniDrama"
  short_description: "Repo-specific patterns and workflows for LocalMiniDrama"
  default_prompt: "Use the LocalMiniDrama repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true